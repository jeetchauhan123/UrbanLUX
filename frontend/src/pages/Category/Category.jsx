import React from "react";
import InProgress from "../components/InProgress";

const Category = () => {
  return (
    <div>
      Category
      <InProgress />
    </div>
  );
};

export default Category;
